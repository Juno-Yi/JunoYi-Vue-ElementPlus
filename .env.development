# 【开发】环境变量

# 应用部署基础路径（如部署在子目录 /admin，则设置为 /admin/）
VITE_BASE_URL = /

# API 请求基础路径（开发环境无前缀，生产环境设置为 /api 等）
VITE_API_URL = /

# API 请求前缀（开发环境为空，生产环境可设置为 /api）
VITE_API_PREFIX = 

# 代理目标地址（开发环境通过 Vite 代理转发请求到此地址，解决跨域问题）
VITE_API_PROXY_URL = http://localhost:7588

# Delete console
VITE_DROP_CONSOLE = false